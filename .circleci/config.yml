version: 2.1
jobs:
  build:
    docker:
      - image: docker:latest
    resource_class: harshfire/mongo
    steps:
      - run:
          name: Start MongoDB container
          background: true
          command: |
            docker run --name mongodb mongo:3.6.22
      - run: echo hello
workflows:
  build-workflow:
    jobs:
      - build
